import { ChangeDetectorRef } from '@angular/core';
import { AsyncPipe } from '@angular/common';
export declare class NodeAsyncPipe extends AsyncPipe {
    _ref: ChangeDetectorRef;
    constructor(_ref: ChangeDetectorRef);
}
